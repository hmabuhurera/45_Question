"use strict";
//Greetings: Start with the array you used in Exercise 11, but instead of just printing each person’s name, print a message to them. The text of each message should be the same, but each message should be personalized with the person’s name.
let Name = ["Quaid-e-Azam", "Allama Iqbal", "Sir Syed Ahmed Khan", "Shah Wali-Ullah"];
//Usinng FOrloop through each array
for (let name of Name) {
    // for of loop
    // print A personlized message for each friend
    console.log(`Hello Sir ${Name}  Hope You are doing very well in the Jinha`);
}
;
for (let i = 0; i < Name.length; i++) {
    const element = "arr";
}
