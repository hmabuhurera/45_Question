//Practice ODD number

let sum:number= 0;
let list:number[] = []

for (let index = 1 ; index <= 100 ; index++){
    if (index % 2 != 0){
        sum = sum + index;
        list.push(index)
    }
}
console.log(`sum ${sum} ${list}`);

//Practice ODD number

let sum1:number= 0;
let list1:number[] = [];

for (let index = 1 ; index <= 100 ; index++) {
    if(index % 2 != 0 ) {
    sum1 = sum1 + index;
    list1.push(index);
    }
}
console.log(`sum ${sum1} ${list1}`);

//practice odd number


let summ:number = 0;
let listt:number[] = [];

for(let index = 1; index <  100 ; index++) {
    if(index % 2 != 0){
        summ = summ + index;
        listt.push(index)
    }
}
  console.log(`sum ${summ} ${listt}`);

  let suum:number=0;
  let lisst:number[] =[];
  
  for(let index =1 ; index <= 100 ; index++) {
    if(index % 2 != 0){
        suum = suum + index;
        lisst.push(index)
    }
  }
  console.log(`sum ${summ} ${listt}`);

  let ssum:number = 0;
  let llist:number[] = [];

  for(let index = 1 ; index< 100 ; index++){
    if(index% 2 != 0){
        ssum = ssum + index;
        llist.push(index)
    }
  }
  console.log(`sum ${summ} ${listt}`);